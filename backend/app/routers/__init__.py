# Routers package — API route handlers
# Routers will be added per phase:
#   Phase 1: payments.py  (orders + payment attempts)
#   Phase 2: simulate.py  (failure injection)
#   Phase 4: engine.py    (decision engine endpoints)
#   Phase 5: audit.py     (log query endpoints)
